import L from '../../common/logger';
export class AuthService{
    loginbByEmail(email: string,password : string): string{
        let token : string = ''
        return token;
    }
    loginbByPhone(phone: string,password : string): string{
        let token : string = ''
        return token;
    }
}

export default new AuthService();
